on:
  push:
    branches:
      - master

name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    environment: Personal
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: Print environment USERNAME variable 
        run: echo ${{ env.FTP_USERNAME }}

      - name: Print environment PASSWORD variable 
        run: echo ${{ env.FTP_PASSWORD }}

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.3.2
        with:
          server:  ftp.chavdamahesh.com
          username: ${{ env.FTP_USERNAME }}
          password: ${{ env.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: /public_html/
